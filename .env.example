# Frontend environment — copy to .env.local.
# The Next.js app holds NO database or AWS credentials; those live in backend/.env.

# Where the standalone backend API server runs
BACKEND_URL=http://localhost:5000

# WhatsApp number orders are sent to (country code + number, no +)
NEXT_PUBLIC_WHATSAPP_NUMBER=917558083325
